# Stock Radar Spark - 베타테스터 가이드

**현재 상태**: 5명 제한 베타 테스트 (2026.02.21)

---

## 🚀 빠른 시작 (개발자용)

### 1. 환경 설정

```bash
# 1. 백엔드 디렉토리로 이동
cd backend

# 2. Python 환경 설정
python -m venv venv
venv\Scripts\activate  # Windows

# 3. 패키지 설치
pip install -r requirements.txt

# 4. 데이터베이스 초기화
python -c "from database import init_db; init_db()"
```

### 2. 서버 시작

```bash
# 개발 서버 (자동 리로드)
python -m uvicorn server_v2:app --reload --host 0.0.0.0 --port 8000

# 또는 배포 서버 (4개 워커)
python -m uvicorn server_v2:app --workers 4 --host 0.0.0.0 --port 8000
```

**접속 URL:**
- 프론트엔드: http://localhost:8000
- API 문서: http://localhost:8000/docs (Swagger UI)
- API 문서: http://localhost:8000/redoc (ReDoc)

---

## 📝 베타테스터 온보딩

### 베타테스터는 다음을 수행합니다:

```bash
# 1. 계정 생성
curl -X POST "http://localhost:8000/api/auth/signup" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "tester_001",
    "email": "tester001@example.com",
    "password": "Test1234!",
    "password_confirm": "Test1234!",
    "full_name": "테스터 1호"
  }'

# 응답:
# {
#   "success": true,
#   "message": "회원가입 성공",
#   "data": {
#     "user_id": 1,
#     "username": "tester_001"
#   }
# }

# 2. 로그인
curl -X POST "http://localhost:8000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "tester_001",
    "password": "Test1234!"
  }'

# 응답 (토큰 저장):
# {
#   "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
#   "token_type": "bearer",
#   "expires_in": 86400,
#   "user": {
#     "id": 1,
#     "username": "tester_001",
#     "email": "tester001@example.com",
#     ...
#   }
# }

# 3. 인증된 요청 (토큰 사용)
curl -X GET "http://localhost:8000/api/me" \
  -H "Authorization: Bearer {위에서_받은_토큰}"

# 4. 프로필 업데이트
curl -X PUT "http://localhost:8000/api/auth/profile" \
  -H "Authorization: Bearer {토큰}" \
  -H "Content-Type: application/json" \
  -d '{
    "risk_profile": "중립",
    "account_size": 1000000,
    "investment_period": "중기"
  }'
```

---

## 🧪 테스트하기

### 1. 시스템 헬시 체크

```bash
curl http://localhost:8000/api/health

# 응답:
# {
#   "status": "healthy",
#   "timestamp": "2026-02-21T10:30:45.123456",
#   "services": {
#     "database": "ok",
#     "ai_agents": "ok",
#     "api_server": "ok"
#   }
# }
```

### 2. 시스템 상태 확인

```bash
curl http://localhost:8000/api/status

# 응답:
# {
#   "app_name": "Stock Radar Spark",
#   "version": "1.0.0-beta",
#   "environment": "development",
#   "active_users": "베타테스터 5명 제한",
#   "services": {...}
# }
```

### 3. AI Agent 분석 실행

```bash
# 토큰으로 보호된 엔드포인트
curl -X POST "http://localhost:8000/api/agents/analyze" \
  -H "Authorization: Bearer {토큰}" \
  -H "Content-Type: application/json" \
  -d '{
    "tickers": ["AAPL", "NVDA"],
    "sector": "AI 반도체"
  }'
```

---

## 🎯 베타테스터 체크리스트

### 회원가입/로그인 테스트
- [ ] 회원가입 성공
- [ ] 비밀번호 유효성 검사 (8자 이상, 대문자+숫자)
- [ ] 이메일 중복 체크
- [ ] 로그인 성공
- [ ] 토큰 발급 확인

### 프로필 관리
- [ ] 프로필 조회 (/api/auth/me)
- [ ] 프로필 업데이트
- [ ] 투자 성향 설정 (보수/중립/공격)
- [ ] 계좌 규모 입력

### AI Agent 기능
- [ ] Market Regime Analyst (시장 상태 분석)
- [ ] Sector Scout (섹터 랭킹)
- [ ] Stock Screener (종목 필터링)
- [ ] Trade Plan Builder (매매계획)
- [ ] Devil's Advocate (반론 생성)

### 성능 & 안정성
- [ ] 5명 동시 접속 테스트
- [ ] 로그인 응답 시간 < 100ms
- [ ] Agent 분석 시간 < 5초
- [ ] 데이터베이스 락 발생 확인
- [ ] 에러 로그 확인

### 데이터 검증
- [ ] 시장 데이터 정확도 (Yahoo Finance)
- [ ] OpenDART API 재무 데이터 확인
- [ ] 신뢰도 점수 정상 범위 (0-100%)

---

## 📊 모니터링 & 피드백

### 주요 확인 사항

1. **성능 문제**
   - API 응답 시간
   - 데이터베이스 lock 발생 빈도
   - 메모리 사용량 (목표: < 500MB)

2. **기능 이슈**
   - Agent 분석 결과 오류
   - 토큰 만료 처리
   - 동시 요청 처리

3. **데이터 정합성**
   - 시장 지표 정확도
   - 종목 분석 결과 검증
   - 역사적 데이터 비교

### 피드백 방법

```
우선순위 | 유형 | 설명
---------|------|------
🔴 Critical | 서비스 다운 | Agent 분석 실패, DB 락
🟠 High | 기능 오류 | 토큰 인증 실패, 데이터 오류
🟡 Medium | 성능 저하 | API 응답 > 1초, 메모리 누수
🟢 Low | 개선 제안 | UI/UX, 기능 추가

## 문제 보고 템플릿
- 발생 시간
- 재현 단계
- 예상 결과 vs 실제 결과
- 브라우저/환경 정보
- 스크린샷 (가능시)
```

---

## 🔒 보안

### 주의 사항

1. **비밀번호**
   - 8자 이상, 대문자+숫자 필수
   - 다른 곳에서 사용하지 않은 패스워드 사용

2. **토큰**
   - 24시간 유효
   - 로그아웃 후 자동 무효화
   - 공유 금지

3. **API Key** (OpenDART)
   - 환경변수에 저장됨
   - 코드에 하드코딩 금지
   - 주기적 로테이션 필요 (상용시)

---

## 📈 로드맵

### Phase 1: 현재 (5명 제한)
- [x] 인증 시스템 (회원가입/로그인)
- [x] 5 AI Agent
- [x] SQLite 데이터베이스
- [x] 기본 모니터링
- [ ] 프론트엔드 UI 연동

### Phase 2: 1개월 후 (50명)
- [ ] PostgreSQL 마이그레이션
- [ ] Redis 캐싱
- [ ] UI 개선
- [ ] 성능 최적화

### Phase 3: 3개월 후 (상용)
- [ ] AWS 배포
- [ ] 자동 스케일링
- [ ] 고급 모니터링
- [ ] 결제 시스템

---

## 🆘 문제 해결

### 로그인 실패
```
문제: "비밀번호가 올바르지 않습니다"
해결: 대소문자 구분, 숫자 확인, CAPS LOCK 확인
```

### 토큰 만료
```
에러: "토큰이 유효하지 않습니다"
해결: 다시 로그인해서 새 토큰 발급
```

### Agent 분석 실패
```
에러: "5 AI Agent 실행 중 오류"
해결: /api/health 확인, 로그 확인
```

### 데이터베이스 락
```
에러: "database is locked"
해결: 다른 사용자 접속 확인, 서버 재시작
```

---

## 📞 연락처

- **문제 보고**: [GitHub Issues](https://github.com/...)
- **일반 문의**: support@stockradar.ai
- **긴급**: crisis@stockradar.ai

---

## 라이선스

Proprietary Beta - 베타테스터만 사용 권한 있음

**마지막 업데이트**: 2026년 2월 21일
